DB_CONNECTION=mysql
DB_HOST=your-db-host.example.com
DB_PORT=3306
DB_DATABASE=your_database
DB_USERNAME=your_user
DB_PASSWORD=your_password

# Optional: change the incidents table name if it's different in your schema
DB_INCIDENTS_TABLE=incidencias

# Copy this file to .env and fill real credentials. Do NOT commit your .env to git.